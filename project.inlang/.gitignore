# Keep the Inlang project lean in Git.
# Settings and source message JSON files are versioned; generated metadata and
# plugin cache files are ignored.
*
!.gitignore
!settings.json
!localization-policy.json
!messages/
!messages/*.json
!drafts/
!drafts/*/
!drafts/*/*.json
